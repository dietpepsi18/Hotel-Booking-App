let express,createConnectedAccount,getAccountStatus,getAccountBalance,payoutSetting,stripeSessionId,stripeSuccess,requireSignin;_90e‍.w("express",[["default",["express"],function(v){express=v}]]);_90e‍.w("../controllers/stripe",[["createConnectedAccount",["createConnectedAccount"],function(v){createConnectedAccount=v}],["getAccountStatus",["getAccountStatus"],function(v){getAccountStatus=v}],["getAccountBalance",["getAccountBalance"],function(v){getAccountBalance=v}],["payoutSetting",["payoutSetting"],function(v){payoutSetting=v}],["stripeSessionId",["stripeSessionId"],function(v){stripeSessionId=v}],["stripeSuccess",["stripeSuccess"],function(v){stripeSuccess=v}]]);_90e‍.w("../middlewares",[["requireSignin",["requireSignin"],function(v){requireSignin=v}]]);//routes for connecting stripe












const router = express.Router();

router.post("/create-connect-account", requireSignin, createConnectedAccount); //requireSignin is middleware
//when send request to this route, need to go through the middleware 'requireSignin' for checking if user has token
//after pass the middleware 'requireSignin', will call 'createConnectedAccount'

router.post("/get-account-status", requireSignin, getAccountStatus);
router.post("/get-account-balance", requireSignin, getAccountBalance);
router.post("/payout-setting", requireSignin, payoutSetting);
router.post("/stripe-session-id", requireSignin, stripeSessionId);

//for creating order:
router.post("/stripe-success", requireSignin, stripeSuccess);

module.exports = router;
